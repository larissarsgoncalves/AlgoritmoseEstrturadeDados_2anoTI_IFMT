function calcularMedia(nota1, nota2) {
    return (nota1 + nota2) / 2;
}

// Exemplo de uso:
let nota1 = 7;
let nota2 = 8;
let media = calcularMedia(nota1, nota2);
console.log(`A média das notas ${nota1} e ${nota2} é: ${media.toFixed(3)}`);
// Saída: A média das notas 7 e 8 é: 7.50
